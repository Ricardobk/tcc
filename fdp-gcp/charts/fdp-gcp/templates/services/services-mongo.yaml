apiVersion: v1
kind: Service
metadata:
  name: fdp-mongo-service
spec:
  selector:
    app.kubernetes.io/name: mongo-fair
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
